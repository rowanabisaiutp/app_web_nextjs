/**
 * Hooks del frontend. Re-exportar aquí para importar desde @/hooks.
 *
 * Uso:
 *   import { useAuth } from "@/hooks";
 *   import { useAuth } from "@/hooks/useAuth";
 */

export { useAuth } from "./useAuth";
